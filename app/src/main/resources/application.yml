spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookly
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate.ddl-auto: none


springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

fenrir:
  app:
    jwtSecret: secret
    jwtExpiration: 86400000